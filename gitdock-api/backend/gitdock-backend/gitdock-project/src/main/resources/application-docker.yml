# Surcharge du profil Docker pour gitdock-project
# Ce fichier est activé par SPRING_PROFILES_ACTIVE=docker

spring:
  datasource:
    url: jdbc:postgresql://gitdock-postgres:5432/${DB_NAME:gitdock_project}

  rabbitmq:
    host: gitdock-rabbitmq

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://gitdock-discovery:8761/eureka/
